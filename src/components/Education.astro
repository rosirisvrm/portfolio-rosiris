---
import EducationItem from "./EducationItem.astro";
import { education } from "../data";
---

<section id="education" class="py-24 px-10 lg:px-32 flex flex-wrap">
    <div class="md:mx-10 w-full flex flex-col">
        
        <h3 class="text-xl font-bold mb-20">| Educación</h3>

        <div class="flex flex-col w-full gap-8 flex-wrap">
            {education.map((edu) => (
                <EducationItem item={edu} />
            ))}
        </div>

    </div>
</section>
